# Decision Log

_Append-only. A superseded decision gets a new dated row rather than an edit —
fitting, for a project about immutable records._

## Product & approach

| Date | Decision |
|---|---|
| 2026-07-23 | **Product direction:** combined platform — metering → rating → invoicing → settlement → reconciliation — built in phases, each independently demoable. |
| 2026-07-23 | **Diverge from the standard resource model.** Ledger-first and policy-driven: what a typical billing API stores as mutable resources (Subscription, Invoice, PaymentIntent), Summa computes as projections over an append-only ledger. |
| 2026-07-23 | **AI integration principle:** AI proposes, the deterministic FP core decides. Generated artifacts (pricing policies, explanations, triage notes) must pass pure validators before anything is committed; AI never mutates state directly. |
| 2026-07-24 | **Build the ledger first** as a thin end-to-end vertical slice deployed on Azure, before designing the remaining contexts in depth. Everything depends on the ledger; the ledger depends on nothing. |
| 2026-07-28 | **Project name: Summa.** Sum types (F#), *Summa de Arithmetica* (Pacioli, 1494), and "the total." |
| 2026-07-28 | **Single public monorepo**, docs versioned alongside code, one PR per build-plan stage. |
| 2026-07-28 | **The AI layer is called Scribe.** A scribe drafts and records on behalf of an authority but has no power to decide — the name states the guardrail. (Avoids collision with Microsoft Copilot.) |

## Stack

| Date | Decision |
|---|---|
| 2026-07-23 | **Backend: F#.** Existing fluency means the learning budget goes to event sourcing and infrastructure rather than syntax; first-class Azure SDK support as a .NET language. |
| 2026-07-23 | **Frontend: TypeScript + Effect.** Chosen deliberately as new ground, on a bounded surface (read-only dashboards) where a learning curve can't compromise the core. |
| 2026-07-23 | **Cloud: Azure.** |
| 2026-07-24 | **Event store: hand-rolled on Azure Database for PostgreSQL**, behind an F# persistence port. Optimistic concurrency, polling subscriptions, and checkpointing are implemented directly — building the mechanics is the point. *Rejected:* Marten (C#-idiomatic, hides the mechanics), Equinox (excellent but imports what we want to learn), KurrentDB (no managed Azure offering). *Escape hatch:* Equinox's MessageDb backend is also Postgres, so the port allows a later swap without touching the domain. |
| 2026-07-24 | **Hosting: Azure Container Apps** — API, projection worker, and a scheduled Job for revenue recognition — with Azure Container Registry, Terraform (over Bicep, for transferability), and GitHub Actions. AKS noted as a deliberate later step; ACA already runs on Kubernetes, so containers and code carry over. |
| 2026-07-29 | **SQL migrations: forward-only, plain `.sql` files** in `db/migrations/`, applied manually via psql for now (Stage 5). A correction is a new migration, never a down script — mirrors the ledger's own append-only, reversing-entry rule rather than introducing a separate rollback mechanism. *Rejected:* DbUp/FluentMigrator/Evolve — same reasoning as the event store decision: hand-roll the mechanics rather than import them. |
| 2026-07-29 | **Migration application: hand-rolled `db/migrate.sh`**, refining the row above. A `schema_migrations(version, applied_at)` table tracks which migrations have run; the script applies each pending file inside its own transaction and skips ones already recorded, so re-running it is safe. Still no DbUp/FluentMigrator/Evolve — this only adds the tracking a plain-file approach was missing. |
| 2026-07-29 | **Postgres driver: Npgsql 10.0.3** (current stable), for the event store adapter in `Summa.Ledger.Store` (Stage 6). |
| 2026-07-29 | **Event payload serialization: FSharp.SystemTextJson 1.4.36** over `System.Text.Json`, configured to unwrap single-case and fieldless-case unions (`AccountId`, `Direction`) into plain JSON values rather than wrapped objects. |
| 2026-07-29 | **`EventStore` port: a record of functions**, not an F# `interface`. `docs/architecture.md`'s "F# port (interface)" wording is read as the architectural (hexagonal) concept, not a literal `interface`-keyword mandate — record-of-functions is more idiomatic F#. |
| 2026-07-29 | **Duplicate-idempotency-key detection: plain `INSERT` + catch Npgsql's unique-violation exception** (`SqlState="23505"`, matched to `ConstraintName`), then a follow-up `SELECT` for the existing row — not `ON CONFLICT DO UPDATE ... RETURNING (xmax = 0)`. Simpler to teach; no Postgres-internals concept required. |
| 2026-07-29 | **Query execution: Npgsql.FSharp 8.0.0** over raw Npgsql (`NpgsqlCommand`/`AddWithValue`/manual reader loop) in `PostgresEventStore.fs`, since the raw ADO.NET style was unusually imperative for F# and more DB work is expected (Stage 9's projection worker). It's a thin pipe-based wrapper, not an ORM — `PostgresException` still propagates unwrapped, so the existing exception-based duplicate detection is untouched. Both `Npgsql` and `Npgsql.FSharp` stay as direct dependencies, since `NpgsqlDataSource`/`PostgresException` are still used directly. |
| 2026-07-30 | **HTTP framework: Falco 5.2.0** for `Summa.Ledger.Api` (Stage 8), over Giraffe. Falco's `HttpHandler = HttpContext -> Task` composed with plain `>>` is the same shape ASP.NET Core minimal APIs already use — less new machinery to teach for two routes than Giraffe's `HttpFunc`/`>=>` continuation-passing layer, which earns its keep chaining many validators/middleware, not here. `Request.getJsonOptions<'T>` (verified against Falco's source) only swallows `NotSupportedException` internally, so a genuine `JsonException` on malformed JSON propagates uncaught — the hook used to give JSON-shape failures a distinct message from domain (`LedgerError`) failures. *Rejected:* Giraffe. |
| 2026-07-30 | **API integration testing: `WebApplicationFactory<Program>`** from `Microsoft.AspNetCore.Mvc.Testing` 10.0.10, in a new `Summa.Ledger.Api.Tests.Integration` project. Boots the real Falco app in-process and drives it with a real `HttpClient`, over the same real local Postgres `Summa.Ledger.Store.Tests` already uses — deliberately not the `ConfigureTestServices`/SQLite-swap pattern Microsoft's own docs show, since this repo's existing integration tests already made the opposite call (prove the real unique-constraint/idempotency mechanics, not a substitute). *Rejected:* hand-rolling a Kestrel-on-a-random-port harness. Required one F#-specific workaround: F#'s type-checker won't accept a `module` as a generic type argument, so `Program.fs`'s `module Summa.Ledger.Api.Program` (which compiles to a class) can't itself fill `WebApplicationFactory<'T>` — a marker `type Program() = class end` was added inside the module, just before `[<EntryPoint>] let main`. F# resolves the same-name collision by nesting the type inside the module's compiled class, which is why the test file opens both `Summa.Ledger.Api` (the namespace) and `Summa.Ledger.Api.Program` (the module) to bring the nested type into scope. |
| 2026-07-29 | **Dedicated `ledger` Postgres schema** for the Ledger bounded context's tables (`ledger.events`, replacing the flat `public.events`), so future contexts (Policy, Rating, Settlement) each own their own schema in the shared `summa` database rather than colliding on table names in `public`. This is bounded-context data ownership (the DDD/microservices "database per service" tradition), a deliberate choice over orthodox event-sourcing's usual answer (one shared physical store, partitioned logically by stream/category) — the two doctrines diverge here and this project picked the former, consistent with its existing per-context project namespacing. `schema_migrations` stays unqualified in `public`, since it's the migration runner's own bookkeeping, not any one context's domain data. Replaced Stage 5's `0001_events_table.sql` outright (deleted, not corrected) since nothing had been deployed yet. |
| 2026-07-30 | **Projection worker (`Summa.Ledger.Projections`, Stage 9): `Microsoft.NET.Sdk.Worker` + `Host.CreateApplicationBuilder`** (the plain Generic Host, not `WebApplicationBuilder` — no HTTP surface here) plus `Microsoft.Extensions.Hosting` 10.0.10. Confirmed by generating a real `dotnet new worker -lang F#` project rather than assuming: the Worker SDK does *not* implicitly bring in `Microsoft.Extensions.Hosting` the way `Sdk.Web` implies ASP.NET Core, and the Generic Host reads `DOTNET_ENVIRONMENT` (not `ASPNETCORE_ENVIRONMENT`) to select `appsettings.{Environment}.json`. Deliberately skips the template's default `BackgroundService`/`AddHostedService<T>`/DI-container shape — `Program.fs` constructs `NpgsqlDataSource`/`EventStore` by hand and calls `Worker.run` directly, matching `Summa.Ledger.Api/Program.fs`'s existing no-DI-container convention rather than introducing one for the first time here. The atomic balance-write + checkpoint-advance uses Npgsql.FSharp's `Sql.executeTransactionAsync` (signature confirmed by reflecting on the installed `Npgsql.FSharp.dll`, not assumed) — a list of `(query, parameter-sets)` pairs run in one transaction — over hand-rolled `NpgsqlConnection`/`BeginTransaction` plumbing. |
| 2026-07-30 | **Deferred: adopt `BackgroundService`/`IHostedService` for the projection worker** (refining the row above) once graceful SIGTERM shutdown actually matters — Stage 11 (containerize) or Stage 13 (Azure Container Apps), whichever comes first, since that's when the deployment platform starts sending SIGTERM before SIGKILL on scale-down/redeploy, and the Generic Host's hosted-service machinery is the standard way to react to that rather than hand-rolling signal handling. Not a correctness requirement — hard-kill safety is already proven unconditionally by the atomic balance-write + checkpoint-advance transaction; this is purely operational (faster/quieter redeploys) plus observability (confirmed: since .NET 6, an unhandled `BackgroundService.ExecuteAsync` exception is logged via `ILogger` before the host stops, vs. today's bare stack-trace-to-stderr with no logging at all). Deliberately not done now, since local `dotnet run` never sends SIGTERM. |
| 2026-07-30 | **Stage 11 Dockerfile shape**: separate Dockerfile per project (`src/Summa.Ledger.Api/Dockerfile`, `src/Summa.Ledger.Projections/Dockerfile`), not one image with an entrypoint switch — the two need different final-stage bases (`aspnet:10.0` for the API's HTTP surface vs. plain `runtime:10.0` for the worker) and will be two independent Container Apps in Stage 13 regardless. Framework-dependent `dotnet publish` for both (the base images already carry the matching `net10.0` runtime; self-contained would duplicate it for no gain). Default Debian-based tags for now, not chiseled — chiseled's missing shell would block `docker exec` debugging on this first pass; `USER $APP_UID` (non-root) still applies either way, so this only defers an image-size optimization, not security. |
| 2026-07-30 | **SIGTERM/`BackgroundService` adoption for the projection worker stays deferred to Stage 13** (refining the row above, which named Stage 11 or Stage 13 as the trigger) — Stage 11 stayed pure-Docker, no F# changes. Verifying Stage 11 surfaced a sharper reason to do it in Stage 13 regardless: starting the worker before `./db/migrate.sh` ran triggered `Worker.fs`'s unhandled `Npgsql.PostgresException`, which printed "Unhandled exception..." but never actually exited the container — `docker top`/`docker inspect` showed the same PID pinned at ~99% CPU indefinitely (`RestartCount` stayed `0`) until manually restarted. No crash-loop, no forward progress, silent resource burn — a more surprising failure mode than a clean exit, and a second, independent reason (beyond graceful shutdown) that Stage 13's adoption is worth doing then. |
| 2026-07-30 | **Stage 14 split into 14a (CI) and 14b (CD)**, each its own workflow file (`ci.yml`/`cd.yml`) rather than one file with conditional jobs — cleaner trigger separation (`pull_request`/push-to-`main` vs. deploy-on-merge), and 14b shouldn't start until 14a is merged and green, matching the "CI before CD" rule the build plan already stated. |
| 2026-07-30 | **NuGet lock files for Stage 14a**: enable `RestorePackagesWithLockFile` (commit `packages.lock.json` per project) rather than skip caching or key `actions/setup-dotnet`'s cache on project-file hashes instead. Costs one `dotnet restore` per project to generate; buys reproducible restores (pinned exact resolved versions, not just ranges) in addition to the caching `setup-dotnet`'s built-in `cache: true` needs a lockfile to key on — reproducibility earns its keep on its own in a project this correctness-focused, caching is a secondary benefit. |
| 2026-07-30 | **GitHub Actions Azure auth: OIDC federated credentials via `azure/login`, never `AZURE_CREDENTIALS`/a client-secret blob** — formalizing `docs/build-plan.md`'s existing Stage 14b guidance into the decision log per the working agreement (item 7); it was already a settled directive, just not yet logged here. `permissions: id-token: write` is required on the job; only `client-id`/`tenant-id`/`subscription-id` are inputs to `azure/login` in this flow, never a `client-secret`. |
| 2026-07-30 | **Terraform module structure confirmed: `infra/modules/` from Stage 12 onward**, not flat `.tf` files — `docs/architecture.md`'s existing "in modules so a later ACA→AKS swap is localized" line stands as literally written. Deliberate exception to the general "no speculative abstraction" working-agreement rule: the module boundary is a specific, already-recorded architecture decision made for a concrete stated reason, not new speculation introduced mid-stage. |
| 2026-07-30 | **Rebuild mechanism (Stage 10): `db/rebuild.sh`**, mirroring `db/migrate.sh`'s existing hand-rolled-script convention, atomically truncates `ledger.account_balances` and resets `ledger.account_balances_checkpoint.last_seq` to `0` — but deliberately does *not* restart `Summa.Ledger.Projections` itself, and rebuild is *not* exposed as an HTTP endpoint. `Worker.run` only reads the checkpoint once at startup and never re-reads it, so resetting DB state while the worker is already running has no observable effect; a full rebuild is necessarily two steps — reset stored state (the script), then restart the worker process, which replays everything via the same `loop`/`applyBatch` machinery Stage 9 already verified. Restarting stays manual: automating "kill and restart a sibling process from a script" is real added complexity for something already provably safe to do by hand, and an unauthenticated HTTP endpoint that truncates a table is a genuine risk to avoid even in a learning project. *Rejected:* `POST /rebuild` endpoint (would need either duplicating `Fold`/apply logic into `Summa.Ledger.Api` or an awkward cross-project reference to `Summa.Ledger.Projections`); a worker that watches for live checkpoint changes and auto-replays (real added complexity, not asked for by this stage). Verified end-to-end: truncated the table, ran the script, restarted the worker, confirmed via `psql` + `diff` that `account_balances` came back byte-identical to a pre-rebuild snapshot. |
| 2026-07-31 | **`BackgroundService`/`IHostedService` adoption for the projection worker, done** (refining the two rows above) — ahead of Stage 13's Terraform being applied, not after. `Program.fs` now calls `builder.Build().Run()` (previously built a `Host.CreateApplicationBuilder` purely for configuration binding and never started it — the host's lifecycle machinery was never active regardless of anything in `Worker.fs`). `Worker.fs`'s `loop`/`run` take a `CancellationToken` threaded through `Task.Delay(pollInterval, stoppingToken)`; a new `Worker.HostedService` type (`inherit BackgroundService()`, override `ExecuteAsync`) adapts `run` to the hosting model. Registered via the `AddHostedService<T>(Func<IServiceProvider,T>)` factory overload — confirmed to live in `Microsoft.Extensions.DependencyInjection`, not `Microsoft.Extensions.Hosting` — since `dataSource`/`eventStore` stay hand-constructed, preserving the existing no-DI-container convention; DI is used only for the hosted-service registration itself. No explicit `BackgroundServiceExceptionBehavior` configuration needed, `StopHost` has been the default since .NET 6. Verified via `docker compose`: `SIGTERM` now produces a clean shutdown (`ExitCode=0`, ~0.3s, `"Application is shutting down..."` logged) instead of Stage 11's observed hang. Noted, deliberately not addressed: on .NET 11 `host.Run()` would rethrow after a `StopHost`-triggered stop (non-zero exit code); on this project's `net10.0` target it still returns normally, so `main` returns `0` even after a crash-triggered stop — out of scope, since the actual goal (graceful shutdown, no hang) is already satisfied. |
| 2026-07-31 | **Stage 13 Container App secrets: ACA's native `secret` block (value from a Terraform variable, never a literal), not Azure Key Vault** — revising `terraform-idioms.md`'s earlier Key Vault preference, drafted before this project had real Container Apps usage to reason about concretely. Key Vault would be genuinely new infrastructure Stage 13 never asked for, and wouldn't actually remove the Postgres password from Terraform state anyway — it's already in state via `azurerm_postgresql_flexible_server.main` regardless of how Container Apps reads it, unless the Key Vault secret were populated entirely outside Terraform (real added operational complexity, not a config toggle). Given the password's in state either way, Key Vault's marginal benefit for this specific secret is smaller than originally assumed. |
| 2026-08-01 | **Key Vault stays deferred for Stage 14b's CD secret flow too** (refining the row above, not overriding it) — CD's `terraform apply` needs the Postgres password, adding a *second* copy (a GitHub Environment secret) alongside the one already in Terraform state and ACA's native secret block. Key Vault could remove that GitHub-stored copy specifically, since CD could fetch the value live via its own OIDC identity instead of a static stored secret — a real but narrow benefit, not the "gets the password out of state entirely" case already rejected above. Still not worth the added infrastructure (a new resource, a new RBAC grant for the CD identity, a new provider/data source) for a project with exactly one secret and one environment. Explicitly not closed the way the row above is: revisit once there are multiple secrets or environments where centralized rotation/audit starts to pay for itself. |
| 2026-08-01 | **Postgres network access stays IP-allowlisted (`var.admin_ip_address`), not private networking** — the real production answer to "how do you reach a database without a fragile firewall rule keyed on your home IP" is a VNet with a private endpoint (`public_network_access_enabled = false` entirely) plus a VPN/Bastion for human admin access, so there's no public IP surface to allowlist in the first place. Not building that here: it means a VNet, subnets, a private endpoint, a VNet-integrated Container Apps environment, and a VPN gateway or Bastion for admin access — real new infrastructure and ongoing cost no stage has asked for, roughly doubling the current infra surface for a single-admin learning project. Known, accepted gap, same shape as the Key Vault rows above: revisit if this project ever needs to model private networking as its own thing. |
| 2026-08-01 | **Container Apps switched from `SystemAssigned` to two separate `UserAssigned` identities** (one per app, each with its own `AcrPull` grant) — discovered during the Stage 12/13/14b bootstrap apply. `SystemAssigned` creates a real ordering deadlock: the identity only exists once the Container App finishes creating, but the `AcrPull` role assignment needs that identity's principal ID, and the Container App's own creation blocks waiting for its first revision to successfully pull the image — which it can't do without the role assignment that can't yet exist. Confirmed directly: zero role assignments existed on the registry after two consecutive failed applies, both timing out with "Operation expired." Pre-creating a dedicated `azurerm_user_assigned_identity` per app, granting `AcrPull` before the Container App exists, and an explicit `depends_on` from each app to its role assignment breaks the cycle, while keeping the same per-app permission isolation `SystemAssigned` gave (deliberately not one shared identity — that would let a compromise of either app inherit whatever the other was ever granted). *Deliberately left open:* revisit switching back to `SystemAssigned` once it's verified that updating an *already-running* Container App's identity type doesn't hit an analogous ordering issue on the new revision — untested, not assumed. |
| 2026-08-01 | **GitHub OIDC federated credential subject uses the immutable format** (`repo:owner@owner_id/repo@repo_id:environment:production`), not the name-only `repo:owner/repo:...` form the module was originally written with — discovered when the first real `cd.yml` run failed OIDC login outright (`AADSTS700213: No matching federated identity record found`). GitHub's [Immutable subject claims for GitHub Actions OIDC tokens](https://github.blog/changelog/2026-04-23-immutable-subject-claims-for-github-actions-oidc-tokens/) (2026-04-23) defaults new repositories to this format specifically so a renamed/recycled username or repo name can't inherit an old trust relationship — `summa` being a new repo, it got the new format automatically, and the module's `subject` just hadn't caught up. Fixed by passing the pre-composed `owner@owner_id/repo@repo_id` string (confirmed against the GitHub API, not just the error message) rather than reverting to the legacy mutable-name toggle, which would mean deliberately choosing the less secure of two options GitHub itself moved away from. |
| 2026-08-01 | **`az containerapp job start --image ...` replaces the container's whole config for that run, not just the image** — discovered when the second real `cd.yml` run got past OIDC but the migration job failed with `docker: command not found`, meaning `db/migrate.sh` had silently fallen back to its local-dev `docker compose exec` path because `PGHOST` (and every other env var Terraform configured on the job) wasn't actually set. Confirmed against `az containerapp job start --help`: `--image`/`--cpu`/`--memory`/`--env-vars` are all "Container Arguments" that override the container spec for that one execution, not additive layers on top of the Terraform-defined template. Fixed by passing `--env-vars` explicitly alongside `--image` in `cd.yml`, including `PGPASSWORD=secretref:postgres-password` to reference the job's already-configured secret rather than re-supplying the raw value from a different source. Verified directly via a manual `az containerapp job start` with the same flags before trusting it in CI. |
| 2026-08-01 | **OIDC module's owner/principal fields stopped deriving from `data.azurerm_client_config.current`/`data.azuread_client_config.current`, now a fixed `local.admin_object_id`** — discovered when the third real `cd.yml` run reached `terraform apply` and failed twice: `azuread_application.github_actions` errored `Insufficient privileges` trying to update its own `owners`, and `azurerm_role_assignment.current_user_tfstate` errored `AuthorizationFailed` trying to delete itself. Root cause: those data sources resolve to *whoever is currently authenticated*, not a fixed identity — a local human apply and a CD service-principal apply produce different values, so each apply tried to "correct" ownership to itself, and CD rightly couldn't win (it can neither modify its own Entra identity nor delete role assignments — both deliberate restrictions from the original design, not gaps). Both failed calls were confirmed atomic — `az ad app owner list`/`az role assignment list` showed nothing had actually changed. Fixed by pinning both to the human owner's real, fixed object ID; `terraform plan` afterward showed zero drift, confirming the fix only corrects the *configuration* to match what was already true, not a live resource change. |
| 2026-08-01 | **`Summa.Ledger.Api`'s Entra ID App Registration skips a custom `identifier_uris` App ID URI entirely, uses the app's own client ID (a GUID) as the JWT audience instead, and sets `api.requested_access_token_version = 2`** — discovered while first applying the `api-auth` module. A custom `api://summa-ledger-api` URI was rejected outright (`InvalidUniqueTenantIdentifierAsPerAppPolicy`): Entra's default tenant policy requires newly-added identifier URIs to embed a verified domain, the tenant ID, or the app's own ID, and the app's own client ID isn't known until after the resource is created — a genuine chicken-and-egg problem for a URI based on it. Using the raw client ID as the audience (and in the client-credentials scope request, `{client_id}/.default`) sidesteps the naming policy entirely and is an equally standard pattern. Separately, without `requested_access_token_version = 2`, issued tokens carried a v1-style `iss` (`https://sts.windows.net/{tid}/`) that didn't match the `https://login.microsoftonline.com/{tid}/v2.0` Authority configured for JWT Bearer validation — verified directly by decoding a real token before writing any application code, not assumed. |
| 2026-08-01 | **Falco's `WebApplication.Run(HttpHandler)` only starts Kestrel if the whole middleware chain stays statically typed as `WebApplication`** — a real bug found while adding `.UseAuthentication()`, not a config mistake. Falco defines its own `WebApplication.Run(handler) : unit` (calls `UseFalcoNotFound` then the real blocking `.Run()`) as an overload distinct from the standard `IApplicationBuilder.Run(RequestDelegate)` (registers terminal middleware, doesn't block, returns instantly). Plain `.UseAuthentication()` is a standard ASP.NET Core extension that returns `IApplicationBuilder`, not `WebApplication` — inserting it into the fluent chain silently narrowed every subsequent call's static type, so `.Run(handler)` resolved to the non-blocking overload and `main` returned `0` immediately with zero output, no exception, no crash. Confirmed by direct elimination (removing `.UseAuthentication()` alone made the process hang/serve correctly again) before finding the real cause in Falco's own source. Fixed using Falco's own `WebApplication.Use(fn: IApplicationBuilder -> IApplicationBuilder) : WebApplication` helper, built for exactly this — slotting in standard middleware without leaving `WebApplication`-typed land. |
| 2026-08-02 | **CD's identity granted the Microsoft Graph `Application.Read.All` application permission** — the exact untested risk flagged when the `api-auth` module was designed materialized on the very next real `cd.yml` run: `terraform plan` failed with `Insufficient privileges` retrieving both `azuread_application` resources, since CD's identity only ever had Azure RBAC `Contributor` (zero Microsoft Graph permissions), and any plan touching the shared root module refreshes every resource in state regardless of what's actually changing. `Application.ReadWrite.OwnedBy` wasn't an option — CD isn't an owner of these App Registrations (only the human is, deliberately, from the earlier ownership-flapping fix) — and Graph doesn't support scoping `Application.Read.All` to specific apps, so the grant is tenant-wide but strictly read-only. Verified directly against the real deployed API afterward (not just a clean `terraform apply`): `/health` open, protected routes 401 with no token, 200 with a real one. |
| 2026-08-06 | **Azure Key Vault adopted for the Postgres admin password** — supersedes the 2026-07-31 and 2026-08-01 rows deferring Key Vault, not an edit to either (both stay as accurate records of the reasoning at the time). The gap those rows didn't anticipate: the password was typed into `TF_VAR_postgres_admin_password` once for the original apply and was never saved anywhere durable — confirmed unrecoverable by anyone. `random_password` now generates it, `azurerm_key_vault` (RBAC-authorized, purge protection on) stores it as a secret, and the two existing consumers (`module.database`, `module.container_apps`) read the generated value directly instead of a variable — so there is nothing left to lose track of. Confirmed directly, not assumed: subscription `Owner` does **not** grant Key Vault data-plane access on an RBAC-authorized vault; a separate `Key Vault Secrets Officer` role assignment (scoped to the vault, granted to the human admin) was required, mirroring the existing `current_user_tfstate` grant. This apply is a deliberate one-time password rotation, the only way to satisfy the `administrator_password` argument at all given the original value's loss. |
| 2026-08-07 | **`infra/` split into three Terraform layers (`infra` foundation, `infra/data`, `infra/compute`); `module.database` and `module.container_apps` destroyed and recreated in the new roots, not `removed`/`import` state surgery.** Root cause: several `infra/modules/github-oidc` resources grant CD's own service principal its permissions, and Azure structurally can't let an identity modify its own authorization — every time one of those needed replacing, CD's apply failed `AuthorizationFailed` and a human had to unblock it locally; separately, the same shared state let CD (or a routine teardown) destroy the live Postgres event log. Splitting so only `infra/compute` is ever CD-applied fixes both. Destroy-and-recreate over `removed`/`import`: nothing in either module holds real data or live traffic yet, so preserving identity across the move isn't worth the complexity (no resource IDs to capture, one `removed`/`import` block per resource, "prove zero drift" verification) — moot anyway, since `terraform state mv` doesn't work against this project's `azurerm` backend. |

## Ledger design

| Date | Decision |
|---|---|
| 2026-07-23 | **Double-entry bookkeeping.** Every transaction balances (debits = credits), so the global "all accounts sum to zero" invariant holds inductively — provable by replay, with no global lock. |
| 2026-07-23 | **Append-only.** Corrections are reversing entries, never edits or deletes. Money is stored as integer minor units; every transaction carries an idempotency key. |
| 2026-07-23 | **Revenue recognition is foundational, not a later feature.** Billing, collection, and recognition are three distinct moments that occur at different times and in different orders; a ledger that conflates them cannot be correct. |
| 2026-07-23 | **Recognition is modeled as a Performance Obligation** (ASC 606 term), created at billing. Policy answers *how much*; the Performance Obligation answers *how it becomes earned over time*. |
| 2026-07-23 | **Recognition mechanism: hybrid.** The schedule is fixed when the obligation is created; an idempotent background job posts each recognition entry as its period comes due; recognized-to-date is also computable directly, and both agree by construction. Not-yet-due recognitions are a plan, not a fact, and stay outside the ledger until due. |
| 2026-07-23 | **Tax Payable and Processing Fee Expense** added to the chart of accounts — recorded now, calculation logic deferred. Both are scheduled-settlement obligations: incurred continuously, discharged on recurring remittance dates. |
| 2026-07-23 | **Ledger accounts are internal bookkeeping categories, not connections to financial institutions.** Mapping internal accounts to external ones and proving they agree is reconciliation's job. The chart of accounts is a defined domain model (fixed types, instances parameterized per customer/currency/jurisdiction), not end-user configuration; posting rules are pure functions, not runtime config. |
| 2026-07-24 | **The Transaction is the consistency unit** — self-validating, immutable, appended to one globally-ordered log. Accounts and balances are projections. *Rejected:* one stream per account, which would require cross-stream atomic writes and break the atomic-balancing guarantee. |
| 2026-07-24 | **One Transaction = one business event.** Bill components (service, tax, fee) are entries, not separate transactions, and land in the transaction for the moment they become known. Related transactions are tied by a shared `CorrelationId`, with `CausationId` recording the immediate cause. |
| 2026-07-24 | **The ledger is unconditional** — it records any balanced transaction. Balance-dependent rules (no-negative, no-double-charge) live above it in the consumers that produce transactions. A rule needing a race-proof guarantee promotes that one account to its own stream with a version check; everything else stays a projection. |
| 2026-07-24 | **Write path:** pure `decide` → idempotent append (unique constraint on idempotency key; global identity sequence for ordering) → async projection. No version check needed on this path, since transactions are independent self-balancing facts. |
| 2026-07-28 | **Read surface for the first slice: one projection table** (`account_balances`). Trial balance and revenue-to-date are queries over it, not separate read models. As-of-date historical queries deferred. |
| 2026-07-30 | **`POST /transactions` returns `200` uniformly**, for both a fresh append and a duplicate-idempotency-key repost, with the same `{"Id": ...}` body — not `201` on first success. A `201` would misleadingly imply a resource was just created on the duplicate path too; one status code keeps both paths honest with the "duplicate is a success, not an error" rule above, instead of leaking the internal `Appended`-vs-`Duplicate` distinction into HTTP status semantics. |
| 2026-07-30 | **`account_balances.balance` uses a uniform "debits − credits" sign convention per account** (Stage 9), with no per-account-type sign flip — `AccountId` stays exactly as opaque as everywhere else in the domain; no chart-of-accounts/account-type concept was introduced. Consequence: credit-normal accounts (Deferred Revenue, Revenue) store negative — verified against the worked example: after invoice + payment + one recognition entry, Cash=12000, AR=0, Deferred Revenue=-11000, Revenue=-1000 (cents). Rationale: since `Transaction.create` already guarantees every posted transaction's debits equal its credits, summing (debits−credits) per account and then across *all* accounts is exactly zero by construction, always — making the Stage 10 trial balance a literal `SUM(balance) = 0` with no type-awareness needed anywhere, matching the "provable trial balance" pitch. *Rejected:* per-account natural-sign balances (Deferred Revenue reading as `+110`), which would require an account-type domain concept ahead of any stage actually asking for one, likely via fragile `AccountId` string-prefix inference, and would break the simple `SUM(balance)=0` trial balance property. |

## Recognition design

| Date | Decision |
|---|---|
| 2026-08-05 | **Performance Obligation is its own bounded context: Recognition** (Stages 15–16, combined). Own projects (`Summa.Recognition.Domain`/`.Store`/`.Job`), own Postgres schema (`recognition`), own small event store (`recognition.obligation_events`, mirroring `ledger.events`'s shape — `Append`/`ReadFrom`, unique `idempotency_key`, `jsonb` payload) — not a plain table bolted onto the `ledger` schema. Rationale: an obligation is not a money fact (`ledger.md`: the ledger is "the append-only record of money facts"; the not-yet-due schedule "is a plan, not a fact"), and schema-per-context was already a deliberate project-wide decision (2026-07-29) worth applying consistently. `PerformanceObligation` also gained its own `IdempotencyKey : string`, separate from `PerformanceObligationId`, mirroring `Transaction`'s `Id`-vs-`IdempotencyKey` split for the same reason (a retried create needs a caller-stable dedup key independent of whether `Id` was regenerated) — and, for the same reason, `CorrelationId`/`CausationId` fields too, mirroring `Transaction`'s exactly. The recognition job forwards `obligation.CorrelationId` unchanged into every `PostTransaction` it posts for that obligation, so the obligation-creation event and every one of its recognition transactions share one true cross-context business correlation id — the same "whole story of a purchase" query `ledger.md` already describes, now spanning both schemas. The schedule (`PerformanceObligation.schedule`) is recomputed live by the job on every run, never materialized into a table — there's nothing to fold (one lifecycle event per obligation), so a projection would only copy the ledger's mechanism without its reason for existing. The recognition job posts into the **existing, unmodified** `ledger.events` via `Summa.Ledger.Store`'s `Handler`/`EventStore` — Recognition produces transactions, it doesn't reimplement the ledger. `AppendResult`/`StoredObligationEvent` are duplicated in `Summa.Recognition.Store` rather than imported from `Summa.Ledger.Store`, deliberately, to avoid the cross-context coupling a shared type would reintroduce. Resolves the open question below. |
| 2026-08-05 | **Recognition schedule rounding: remainder lands entirely on the final period.** Each of the first `PeriodCount − 1` periods gets `TotalAmount / PeriodCount` (integer floor); the last period gets that same base amount plus whatever's left over. E.g. $100 over 3 months (cents): 3333, 3333, 3334 — sum is exactly 10000. Resolves the build-plan's open "decide and test the rounding rule" item for Stage 15. *Rejected:* spreading the remainder across the first N periods (one extra cent each) — also total-preserving, but a less obvious rule for the same benefit. |

## Open questions

- **Chart of accounts** — credits/promotions, write-offs, multi-currency.
- Remaining recognition timing cases: pay-monthly (arrears), pay-after-delivery.
- Multi-currency — single currency to start, or currency as a dimension?
- Per-customer account dimensions and as-of-date queries (deferred from the
  first slice).
- Frontend scope per phase.
